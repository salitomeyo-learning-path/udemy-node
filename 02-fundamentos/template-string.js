const name = "Deadpool";
const real_name = "Wade Winston";

const normal = name + " " + real_name;
const template = `${name} ${real_name}`;

const html = `
<h1>hola</h1>
<p>El backtick permite multilinea</p>
`

console.log(normal);
console.log(template);
console.log(html);

